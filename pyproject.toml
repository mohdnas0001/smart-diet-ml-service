[project]
name = "smart-diet-ml-service"
version = "1.0.0"
description = "AI-powered Food Recognition & Nutrient Estimation microservice"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
